$scriptPath = split-path -parent $MyInvocation.MyCommand.Definition
$jarPath = join-path -Path $scriptPath -ChildPath "licensemanager.jar"

$p = Start-Process -FilePath java -ArgumentList "-jar", "$jarPath" -Wait -Verb runas -PassThru

if ($p.ExitCode -gt 1) { 
    exit $p.ExitCode
}

exit 0